# Scout app
streamlit>=1.35
pandas>=2.0
numpy>=1.26
altair>=5.0
python-dotenv>=1.0
pyyaml>=6.0
supabase>=2.0
httpx>=0.27
anthropic>=0.69  # LLM core (agents/); inference_geo requires a recent SDK

# Only needed to retrain the growth model (ml/train_growth_model.py)
xgboost>=2.0
scikit-learn>=1.4
scipy>=1.13

# Dev
pytest>=8.0
